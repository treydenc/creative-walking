# Copy to .env.local and fill in. Never commit the filled-in version.

# Where the glasses backend's API server is reachable.
# Defaults to port 3001 (APP_CONFIG.apiPort in glasses/).
# NEXT_PUBLIC_ is exposed to the browser — never put a secret here.
NEXT_PUBLIC_API_URL=http://localhost:3001
